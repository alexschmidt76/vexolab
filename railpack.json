{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "lts"
  },
  "steps": {
    "install": {
      "commands": ["pwd && ls -la && npm --prefix server install"]
    },
    "build": {
      "commands": ["npm --prefix server run build"]
    }
  },
  "deploy": {
    "startCommand": "node server/dist/server/index.js"
  }
}
